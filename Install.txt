# Installer d'abord NodeJS (si c'est pas encore fait) : https://nodejs.org

# Vérifier que les commandes 'node' et 'npm' marchent :
node -v 
npm -v

# Installer Angular (si c'est pas encore fait) :
npm install -g @angular/cli

# Nouveau projet :
ng new nom_projet

No
css
N

# Récupération d'un projet existant depuis Git :
npm install

# Vérifier version :
ng version

## On peut aussi regarder dans le fichier package.json à la ligne : "@angular/core": "^18.2.0",

# Démarrer serveur :
ng serve
npm start

# Démarrer serveur et ouvrir automatiquement dans navigateur :
ng serve --open

# Création d'un nouveau composant :

ng generate component partials/header
ng generate component partials/footer
ng generate component pages/home
ng generate service services/article
ng generate component pages/article

# Aller sur : http://localhost:4200/

# Compiler :
ng build

Récupérer ensuite le contenu du dossier "dist/nom-projet/browser/" créé et le placer à la racine d'un serveur.


# ----- Installer json-server -----

npm install json-server
npx json-server db.json

ng g s services/book

ng generate component components/booklist

ng serve


# ==================== Ajouter CSS ====================

Ajouter CSS dans : src/styles.css